"""Environment-specific Django settings.

Set ``DJANGO_SETTINGS_MODULE`` explicitly outside local development. The Docker
image defaults to the production module, while Compose selects development.
"""
