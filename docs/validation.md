# Validation log

Initial local verification on 2026-09-15:

| Check | Result |
| --- | --- |
| Workspace inventory | Empty at start; no existing files overwritten |
| GitHub branches/tags/PRs/forks | Read-only inventory completed |
| Dump metadata | Custom dump, 19 data markers; temporary file removed |
| Frontend TypeScript + Vite build | Passed |
| Frontend ESLint | Passed |
| Frontend Vitest | One CSRF/session request test passed |
| npm dependency audit after Vitest update | Zero reported vulnerabilities at install time |
| Python `manage.py check` | Passed with isolated GDAL/GEOS libraries; no database connection needed |
| Ruff lint/format | Passed after formatting |
| Django migrations | Generated and applied to fresh isolated PostGIS database |
| Backend pytest with PostGIS | 13 passed against disposable PostGIS service |
| Docker Compose startup | Isolated project `gda-modernisation-validation-20260915` started; database, Redis and backend healthy; frontend served on port 8080 |
| Local HTTP smoke checks | Home, health, category catalog, schema, docs and admin login returned HTTP 200 |
| Local Gitleaks scan | Zero findings with redacted output |
| Visual browser inspection | Incomplete: the computer-use tool stopped when it could not establish the browser URL |
| Initial GitHub Actions run | [Run 35032395135](https://github.com/ricardoportoIE/gda-environmental-reporting-platform/actions/runs/35032395135): backend and frontend passed; Gitleaks action failed to form a Git range before the root commit, without scanning the history |
| Follow-up GitHub Actions run | [Run 35032770246](https://github.com/ricardoportoIE/gda-environmental-reporting-platform/actions/runs/35032770246): backend, frontend and complete-history Gitleaks scan all passed |

The test database contains only data generated by migrations and tests. The legacy dump was not restored. The secret-scan workflow now runs a verified Gitleaks binary over the complete history. These checks do not prove that a live deployment is ready.

## Version 2 validation on 2026-09-16

| Check | Result |
| --- | --- |
| Backend tests and coverage | 19 passed against a fresh isolated PostGIS database; 87% branch-aware coverage |
| Spatial radius query | PostGIS distance filtering, ordering, role restriction and input bounds passed |
| Demo data command | Dry-run reported four records; first run created four; second run created zero |
| Frontend component tests | 16 passed across five files |
| Frontend coverage | 53.13% lines, 51.76% statements, 51.33% branches and 39.13% functions; configured thresholds passed |
| Frontend build and lint | TypeScript/Vite build and ESLint passed |
| Playwright E2E | Two Chromium-compatible Edge headless journeys passed against isolated Compose on port 8081 |
| Anonymous access regression | A reload now clears the one-time code from browser history state and requires it to be entered again |
| Dashboard cache regression | A newly submitted authenticated report appears when returning to the report list |

The E2E suite blocks OpenStreetMap tile requests during automation to avoid consuming the community tile service. CI installs Playwright Chromium and runs the same journeys against a fresh Compose stack.

The public [version 2 validation run](https://github.com/ricardoportoIE/gda-environmental-reporting-platform/actions/runs/35068858578) passed the backend, frontend, E2E and complete-history secret-scan jobs for commit `7707ddb`. Image publishing was correctly skipped because this run was not triggered by a version tag.
